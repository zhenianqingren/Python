import jieba
# jieba.lcut(s) precise mode to return a word segmentation result of list type
# jieba.lcut(s,cut_all=True) full mode, returns a word segmentation result of list type, with redundancy
# jieba.lcut_for_search(s) search engine mode, which returns a word segmentation result of list type, with redundancy
# jieba.add_word(w)
j=1.0
for i in range(10):
    print(j)
    j=j+1